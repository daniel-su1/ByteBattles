#include <iostream>
#include <vector>
#include <memory>
#include "link.h"
#include "data.h"
#include "virus.h"
#include "player.h"

int main() {
    // char s;
    // std::vector<std::unique_ptr<Link>> links;
    // while (std::cin >> s) {
    //     int strength;
    //     if (s == 'v') {
    //         std::cin >> strength;
    //         links.emplace_back(std::make_unique<Virus>(strength, (1,1), a, ));
    //     } else if (s == 'd') {
    //         std::cin >> strength;
    //         links.emplace_back(std::make_unique<Data>(strength));
    //     } else {
    //         break;
    //     }
    // }
    // for (const auto& link : links) {
    //     if (link->getType() == virus) {
    //         std::cout << "VIRUS, strength:";
    //     } else {
    //         std::cout << "DATA, strength:";
    //     }
    //     std::cout << link->getStrength() << std::endl;
    // }
}
